# Workflows

Each subdirectory under this folder is a standalone analysis workflow.

- `scrna/`: single-cell RNA-seq
- `bulk-rna/`: bulk RNA-seq
- `microbiome/`: microbiome analysis
- `spatial/`: spatial transcriptomics

Run scripts from within the corresponding workflow directory so that relative file paths continue to resolve.
